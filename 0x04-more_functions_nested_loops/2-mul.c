#include "main.h"
/**
 * mul - multiplies two integers
 * @a: to first number to multiply
 * @b: to sencond number to multiply
 * Return: the result of multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
